# מסמך הסכמה - רשימת קניות חכמה

## מה זה?
אפליקציית ווב לניהול רשימת קניות, עם אפשרות לסמן פריטים כ"נקנו".

## למי זה מיועד?
לשימוש אישי/משפחתי - מי שרוצה לנהל קניות בלי פתקים על הדלת.

## מה חייב להיות בגרסה הראשונה (חובה):
1. הוספת פריט לרשימה (שם + כמות)
2. סימון פריט כ"נקנה" (קו חוצה או צבע שונה)
3. מחיקת פריט מהרשימה
4. הרשימה נשמרת גם אחרי רענון הדף (localStorage)

## מה נחמד שיהיה, אבל לא היום (עתידי):
1. חלוקה לקטגוריות (ירקות, חלב, ניקיון...)
2. שיתוף הרשימה עם בן/בת זוג
3. הצעות אוטומטיות לפי היסטוריית קניות
4. אפליקציית מובייל (במקום דפדפן)

## איך זה ייראה?
נקי ופשוט, צבע אחד דומיננטי, פונט קריא, מתאים גם למסך נייד.

## מה בהחלט לא כלול:
בלי התחברות משתמשים, בלי חיבור לאפליקציות קניות חיצוניות,
בלי מסד נתונים בענן - שמירה מקומית בדפדפן מספיקה.

---

## פרומפט פתיחה (להעתיק ל-Claude Code)

```
זה מסמך ההסכמה של הפרויקט שלי. אני מתחיל/ה כרגע, בלי שום קוד קיים.

[המסמך למעלה]

תבנה לי גרסה ראשונה שעובדת, פשוטה ככל האפשר, שמכסה רק את
"מה חייב להיות בגרסה הראשונה". אל תוסיף פיצ'רים שלא ביקשתי.
תסביר לי בסוף בכמה משפטים פשוטים מה בנית ואיך להריץ את זה.
```
